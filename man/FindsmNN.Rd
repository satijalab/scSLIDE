% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/clustering.R
\name{FindsmNN}
\alias{FindsmNN}
\title{Find Single-Modality Nearest Neighbors}
\usage{
FindsmNN(
  object,
  sketch.assay = NULL,
  reduction,
  dims = NULL,
  k.nn = 20,
  l2.norm = TRUE,
  nn.name = "single.nn",
  verbose = TRUE,
  ...
)
}
\arguments{
\item{object}{A Seurat object}

\item{sketch.assay}{The name of the sketched assay of the landmark cells.}

\item{reduction}{The name of a single dimensional reduction to use (e.g. \code{"proj.pls"}).}

\item{dims}{The dimensions of the reduction to use. If \code{NULL} (default), all
dimensions are used.}

\item{k.nn}{The number of nearest neighbors to compute for each cell. Honored as
given (unlike \code{\link{FindmmNN}}, which clamps to a minimum of 20).}

\item{l2.norm}{Perform L2 normalization on the cell embeddings}

\item{nn.name}{Name under which the resulting Neighbor object is stored.}

\item{verbose}{Print progress bars and output}

\item{...}{Arguments passed to \code{Seurat::FindNeighbors}}
}
\value{
return a Seurat object that contains a Neighbor object between the landmark cells and all the other cells.
}
\description{
A wrapper function that builds a Neighbor object from a single reduction
(e.g. the PLS embedding), mapping every cell to its nearest landmark cells.
}
\details{
This is the single-modality counterpart of \code{\link{FindmmNN}}: instead of
combining several reductions with WNN weights, it runs a single
\code{Seurat::FindNeighbors} call on one reduction, with the landmark (sketched)
cells as the reference and all cells as the query. The resulting Neighbor object
is a drop-in replacement for the \code{weighted.nn} produced by \code{FindmmNN}
and is consumed by \code{\link{GenerateSampleObject}} in the same way.
}
\concept{multimodal}
