% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/normalization.R
\name{NormalizeChiSquared}
\alias{NormalizeChiSquared}
\alias{NormalizeChiSquared.default}
\alias{NormalizeChiSquared.Assay}
\alias{NormalizeChiSquared.StdAssay}
\alias{NormalizeChiSquared.Seurat}
\title{Apply Chi-squared normalization to a Seurat object}
\usage{
NormalizeChiSquared(object, ...)

\method{NormalizeChiSquared}{default}(object = NULL, verbose = TRUE, ...)

\method{NormalizeChiSquared}{Assay}(object, verbose = TRUE, ...)

\method{NormalizeChiSquared}{StdAssay}(
  object,
  layer = "counts",
  save = "data",
  verbose = TRUE,
  ...
)

\method{NormalizeChiSquared}{Seurat}(object, assay = NULL, verbose = TRUE, ...)
}
\arguments{
\item{object}{a count matrix (a landmark-sample density matrix), Assay, StdAssay, or Seurat object}

\item{...}{Additional arguments}

\item{verbose}{display progress bar for normalization procedure}

\item{layer}{Layer to use (for StdAssay objects)}

\item{save}{Layer to save results to (for StdAssay objects)}

\item{assay}{Name of assay to use (for Seurat objects)}
}
\value{
Normalized object of the same class as input
}
\description{
This function applies Chi-squared normalization to a (sample-level) Seurat object. It calculates the expected density (count of cells) for each
landmark-sample pair assuming random distribution, then normalizes observed counts using:
(observed - expected) / √(expected). The resulting values show how much each landmark's density deviates
from the expected baseline—density values. Positive values indicate higher-than-expected density,
negative values indicate lower-than-expected expression.
}
\examples{
\dontrun{
# Normalize a sample-level Seurat object
sample_obj <- NormalizeChiSquared(sample_obj)
}

}
\concept{normalization}
