% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/predict_pls.R
\name{PredictPLS}
\alias{PredictPLS}
\alias{PredictPLS.DimReduc}
\alias{PredictPLS.Seurat}
\title{Predict Responses from a PLS Model}
\usage{
PredictPLS(object, newdata, ...)

\method{PredictPLS}{DimReduc}(object, newdata, ncomp = NULL, ...)

\method{PredictPLS}{Seurat}(
  object,
  newdata,
  reduction = "pls",
  ncomp = NULL,
  assay = NULL,
  layer = "data",
  ...
)
}
\arguments{
\item{object}{A \code{DimReduc} object or \code{Seurat} object containing
a PLS model fitted with \code{save.model = TRUE}.}

\item{newdata}{New observations: a cells x features numeric matrix (for
the DimReduc method) or a Seurat object (for the Seurat method).}

\item{...}{Additional arguments (currently unused).}

\item{ncomp}{Integer vector of component counts to predict with. Each
value produces a separate set of predictions using the cumulative
coefficients for that many components. Default: maximum available.}

\item{reduction}{(Seurat method only) Name of the PLS reduction slot.
Default \code{"pls"}.}

\item{assay}{(Seurat method only) Assay to use from \code{newdata}.
Default: \code{DefaultAssay(newdata)}.}

\item{layer}{(Seurat method only) Layer to use from the assay. Default
\code{"data"}.}
}
\value{
A matrix (n x q) if a single \code{ncomp} value, or a 3D array
(n x q x length(ncomp)) if multiple values are given.
}
\description{
Performs response prediction on new data using a PLS model previously
fitted with \code{\link{RunPLS}(..., save.model = TRUE)}. Mirrors the
core behaviour of \code{predict.mvr()} from the \pkg{pls} package.
}
\examples{
\dontrun{
# Fit PLS model with save.model = TRUE
obj <- RunPLS(obj, Y = "response", save.model = TRUE)

# Predict on new Seurat object
predictions <- PredictPLS(obj, newdata = new_obj)
}
}
