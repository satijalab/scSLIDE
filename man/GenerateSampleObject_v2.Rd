% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sample_level_object.R
\name{GenerateSampleObject_v2}
\alias{GenerateSampleObject_v2}
\title{Generate a sample-level count matrix with cell-type proportion harmonization across batches}
\usage{
GenerateSampleObject_v2(
  object,
  integrate_key,
  condition_key,
  group.by = "ident",
  cell_type_key,
  seed = 42,
  verbose = TRUE,
  nn.name = NULL,
  k.nn = 5,
  sketch.assay = "LANDMARK",
  return.seurat = TRUE,
  new_assay_name = "LMC",
  normalization.method = "ChiSquared",
  scale.factor = 10000,
  rename.group.by = NULL,
  add.meta.data = TRUE,
  remove.sketch.cell.from.col = TRUE,
  ...
)
}
\arguments{
\item{object}{Seurat object (must already contain a Neighbor object, e.g.
from \code{\link{PrepareSampleObject}})}

\item{integrate_key}{Metadata column identifying the batch (technical
replicate / sequencing run)}

\item{condition_key}{Metadata column identifying the biological condition
(e.g. disease vs. control)}

\item{group.by}{Metadata column identifying sample identity (e.g. donor).
Default is \code{"ident"}.}

\item{cell_type_key}{Metadata column identifying the cell-type annotation}

\item{seed}{Random seed for reproducible downsampling. Default is 42.}

\item{verbose}{Print progress and diagnostic messages. Default is TRUE.}

\item{nn.name}{Name of the Neighbor object. If NULL, inferred from the
Seurat object.}

\item{k.nn}{Number of nearest neighbours used for aggregation. Default is 5.}

\item{sketch.assay}{Name of the sketch assay. Default is \code{"LANDMARK"}.}

\item{return.seurat}{Whether to return a Seurat object. Default is TRUE.}

\item{new_assay_name}{Name for the new assay. Default is \code{"LMC"}.}

\item{normalization.method}{Normalization method. Default is
\code{"ChiSquared"}.}

\item{scale.factor}{Scale factor for Log-Normalization. Default is 10 000.}

\item{rename.group.by}{Metadata column used to rename landmark rows.}

\item{add.meta.data}{Attach sample-level metadata. Default is TRUE.}

\item{remove.sketch.cell.from.col}{Remove landmark cells from columns.
Default is TRUE.}

\item{...}{Additional arguments passed to
\code{\link{GenerateSampleObject}}.}
}
\value{
The return value of \code{\link{GenerateSampleObject}} (a Seurat
object or matrix), built from the harmonized subset of cells.
}
\description{
Wraps \code{\link{GenerateSampleObject}} with a pre-processing step that
downsamples cells so that cell-type proportions are harmonized across batches
within each condition. For each condition, the target proportion for each cell
type is the median across batches. A bottleneck-based downsampling is then
applied per (condition, batch) to match those target proportions as closely as
possible.
}
\concept{scSLIDE}
